{
  "scripts": {
    "start": "concurrently \"npm run web\" \"npm run scss\"",
    "web": "clear && live-server src --quiet",
    "scss": "sass --watch src/index.scss src/index.css --no-source-map",
    "test": "npx karma start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "jasmine-core": "^5.0.1",
    "karma": "^6.4.2",
    "karma-chrome-launcher": "^3.2.0",
    "karma-jasmine": "^5.1.0",
    "live-server": "^1.1.0",
    "sass": "^1.63.6"
  },
  "dependencies": {
    "d3": "^7.8.5"
  }
}
